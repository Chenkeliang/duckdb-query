# 重启服务并测试

## 🚀 立即执行

### 1. 重启后端服务（必须）⚠️

后端代码已修改，必须重启：

```bash
# 在后端终端中
cd api
# 按 Ctrl+C 停止当前服务
# 然后重新启动
python -m uvicorn main:app --reload
```

### 2. 刷新浏览器

前端已自动热更新，刷新浏览器即可：
```
按 F5 或 Cmd+R 刷新页面
```

---

## ✅ 快速验证（2 分钟）

### 测试 1: 数据库连接展开
```
1. 打开 DataSource Panel
2. 找到 "meepo" 数据库连接
3. 点击展开
4. ✅ 验证: 显示 schema 列表（不再是"暂无数据"）
```

### 测试 2: 搜索特殊字符
```
1. 在搜索框输入: (
2. ✅ 验证: 不崩溃
3. 输入: [
4. ✅ 验证: 不崩溃
```

### 测试 3: 右键菜单
```
1. 右键点击数据库连接
2. ✅ 验证: 菜单弹出
3. 点击"刷新此连接"
4. ✅ 验证: 显示成功提示
```

---

## 🐛 如果还有问题

### 问题 1: 后端启动失败
```bash
# 检查端口是否被占用
lsof -i :8000
# 杀死占用进程
kill -9 <PID>
```

### 问题 2: 前端仍然 404
```bash
# 清除浏览器缓存
# Chrome: Cmd+Shift+Delete
# 或者硬刷新: Cmd+Shift+R
```

### 问题 3: 数据库连接仍显示"暂无数据"
```
1. 检查后端日志是否有错误
2. 检查数据库连接配置是否正确
3. 尝试右键"刷新此连接"
```

---

## 📊 修复的问题清单

- [x] 后端导入错误（`database_manager` → `db_manager`）
- [x] API 路径缺少 `/api` 前缀（4 处）
- [x] 前端 6 个问题（右键菜单、颜色、删除、搜索等）

**总计**: 11 个问题全部修复 ✅

---

**重要**: 必须重启后端服务才能生效！
