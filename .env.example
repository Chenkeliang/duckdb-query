# ===================================================================
#        Interactive Data Query - Environment Variables
#                  交互式数据查询 - 环境变量
# ===================================================================
#
# Instructions:
# (使用说明:)
# 1. Copy this file to a new file named `.env`
#    (1. 将此文件复制为一个名为 .env 的新文件)
# 2. Fill in the necessary values below.
#    (2. 填入以下所需的值)
#
# ===================================================================

# -------------------------------------------------------------------
# Backend API Configuration (Required)
# (后端 API 配置 - 必需项)
# -------------------------------------------------------------------

# A secret key is crucial for security. 
# (密钥对于安全至关重要)
# Generate a long, random string and paste it here.
# (请生成一个长且随机的字符串并粘贴至此)
# You can use an online generator or run: openssl rand -hex 32
# (您可以使用在线生成器, 或运行命令: openssl rand -hex 32)
SECRET_KEY=your-super-secret-key-that-is-long-and-random

# -------------------------------------------------------------------
# MySQL Database Connection (Optional)
# (MySQL 数据库连接 - 可选项)
# -------------------------------------------------------------------
#
# If you want to connect to your own MySQL database, fill these in.
# (如果您希望连接到自己的 MySQL 数据库, 请填写以下变量)
# You can add more connections by creating a `mysql-configs.json` file.
# (您也可以通过创建一个 `mysql-configs.json` 文件来添加更多连接)
#
# MYSQL_HOST=your-database-host.com
# MYSQL_PORT=3306
# MYSQL_USER=your-database-user
# MYSQL_PASSWORD=your-database-password
# MYSQL_DB=your-database-name

# -------------------------------------------------------------------
# Performance & Limits (Optional - Defaults are sensible)
# (性能与限制 - 可选项, 默认值通常是合理的)
# -------------------------------------------------------------------

# Maximum size for file uploads (in bytes). Default: 104857600 (100 MB)
# (文件上传的最大体积, 单位为字节。默认值: 104857600, 即 100 MB)
# MAX_FILE_SIZE=104857600

# Query timeout in seconds. Default: 300
# (查询超时时间, 单位为秒。默认值: 300)
# QUERY_TIMEOUT=300

# Maximum rows to return in a single query. Default: 10000
# (单次查询返回的最大行数。默认值: 10000)
# MAX_QUERY_ROWS=10000

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL). Default: INFO
# (日志级别。可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL。默认值: INFO)
# LOG_LEVEL=INFO
