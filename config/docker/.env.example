# Docker Compose 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# 项目根目录路径（相对于 config/docker 目录）
# 默认值：../.. （假设从 config/docker 目录执行）
# 如果从其他目录执行，请设置为项目根目录的绝对路径
PROJECT_ROOT=../..

# 或者使用绝对路径（推荐用于生产环境）
# PROJECT_ROOT=/path/to/interactive-data-query

# API端口
API_PORT=8000

# 前端端口
FRONTEND_PORT=3000

# 环境类型
ENV=development
