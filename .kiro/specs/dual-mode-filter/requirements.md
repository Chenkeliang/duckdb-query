# 双模筛选器需求文档 (Dual-Mode Filter)

> **版本**: 1.0  
> **创建时间**: 2024-12-23  
> **状态**: 📋 需求收集阶段

---

## 📋 需求概述

为 JOIN Query Panel 实现一个**双模 WHERE 条件构建器**，使用户能够：

1. **可视化模式 (Visual Builder)** - 通过拖拽、点击的方式构建过滤条件
2. **SQL 模式 (Expression Mode)** - 直接编写 WHERE 子句
3. **双向同步** - 两种模式可无缝切换，状态自动同步

---

## 🎯 用户故事

### 故事 1: 简单筛选
> 作为数据分析师，我想在 JOIN 查询中快速添加筛选条件（如 `status = 'active'`），而不需要手写完整的 SQL。

**验收标准**:
- [ ] 在 TableCard 的列名旁，鼠标悬停显示筛选图标
- [ ] 点击图标弹出条件配置 Popover
- [ ] 选择操作符（=, >, <, LIKE, IN 等）并输入值
- [ ] 点击"添加"后，条件出现在底部 FilterBar

### 故事 2: 多值筛选 (IN)
> 作为用户，我想筛选某个字段等于多个值的情况（如 `city IN ('北京', '上海', '广州')`）。

**验收标准**:
- [ ] 操作符下拉菜单包含 `IN` 和 `NOT IN`
- [ ] 选择 `IN` 后，输入框变为 Tags Input（支持多值输入）
- [ ] 生成的 SQL 使用标准 `IN (...)` 语法

### 故事 3: 复杂逻辑组合
> 作为高级用户，我需要构建 `(A AND B) OR C` 这样的嵌套逻辑。

**验收标准**:
- [ ] 条件之间显示逻辑连接符（AND/OR），点击可切换
- [ ] 支持将多个条件拖拽组合成一个分组
- [ ] 分组内部和外部可以有不同的逻辑连接符
- [ ] 视觉上明确显示分组层级（如缩进或边框）

### 故事 4: SQL 模式
> 作为熟悉 SQL 的用户，我希望直接编写复杂的 WHERE 子句，而不受可视化构建器的限制。

**验收标准**:
- [ ] 提供"切换到 SQL 模式"按钮
- [ ] 切换后显示 SQL 编辑器，内容为当前可视化条件的 SQL
- [ ] 支持直接编辑 SQL
- [ ] 切换回可视化模式时，尝试解析 SQL 并还原结构

### 故事 5: 解析失败降级
> 当我输入的 SQL 过于复杂（如包含子查询），系统应能安全处理。

**验收标准**:
- [ ] 无法解析的 SQL 部分显示为"原始 SQL 块"
- [ ] "原始 SQL 块"可以和普通条件混合使用
- [ ] 生成最终 SQL 时，原始块内容原样保留

---

## 🔒 边界条件与约束

### 功能边界

| 场景 | 处理方式 |
|------|----------|
| 空条件 | 不生成 WHERE 子句 |
| 单条件 | `WHERE col = val` |
| 多条件同逻辑 | `WHERE A AND B AND C` |
| 嵌套逻辑 | `WHERE (A OR B) AND C` |
| 复杂函数 | 降级为 Raw SQL 块 |
| 子查询 | 降级为 Raw SQL 块 |

### 特殊字符处理

| 场景 | 处理方式 | 示例 |
|------|----------|------|
| 表名/列名含空格 | 用双引号包裹 | `"user name"."first name"` |
| 表名/列名含中文 | 用双引号包裹 | `"用户表"."姓名"` |
| 表名/列名含引号 | 双引号转义为两个双引号 | `"table""name"` |
| 值含单引号 | 单引号转义为两个单引号 | `'O''Brien'` |
| 空值 NULL | 使用 IS NULL / IS NOT NULL | `col IS NULL` |
| 空字符串 | 使用 `= ''` | `col = ''` |

### 数据类型校验

| 列类型 | 校验规则 | 错误提示 |
|--------|---------|---------|
| INTEGER, BIGINT | 必须为整数 | "请输入有效整数" |
| DOUBLE, DECIMAL | 必须为数字 | "请输入有效数字" |
| DATE | 格式 YYYY-MM-DD | "日期格式无效，请使用 YYYY-MM-DD" |
| TIMESTAMP | ISO 8601 格式 | "时间格式无效" |
| BOOLEAN | true/false | "请输入 true 或 false" |
| VARCHAR | 任意字符串 | - |

### 技术约束

- **技术栈**：React, TypeScript, Shadcn UI, TailwindCSS
- **编辑器**：复用现有 `SQLEditor` 组件（CodeMirror）
- **拖拽库**：推荐 `@dnd-kit/core`（轻量、React 友好）
- **解析策略**：自建轻量解析器，不引入重型 SQL Parser 库

### 性能约束

| 指标 | 要求 | 超出处理 |
|------|------|---------|
| 条件数量上限 | 建议 50 个 | Toast 提示使用 SQL 模式 |
| 嵌套层级上限 | 建议 5 层 | 超出时禁止继续嵌套 |
| 渲染性能 | 100 条件以内无卡顿 | 50+ 条件使用虚拟滚动 |
| SQL 解析超时 | 100ms | 超时降级为 Raw 块 |
| 模式切换防抖 | 300ms | 避免频繁切换 |

---

## 🚨 错误处理规范

### 错误类型与处理

| 错误类型 | 检测时机 | 处理方式 | 用户提示 |
|---------|---------|---------|---------|
| SQL 注入尝试 | 值输入时 | 自动转义，记录日志 | 无（静默处理） |
| 解析失败 | SQL → Visual 切换 | 降级为 Raw 块 | Toast: "部分条件无法解析，已保留原始 SQL" |
| 类型不匹配 | 值输入时 | 高亮错误字段，阻止提交 | "该列为数值类型，请输入有效数字" |
| 空条件提交 | 点击确定时 | 阻止提交 | "请至少填写一个条件" |
| 超长字符串 | 值输入时 | 截断并提示 | "输入内容过长，已截断至 1000 字符" |
| 嵌套层级过深 | 拖拽分组时 | 阻止操作 | "分组层级不能超过 5 层" |
| 条件数量过多 | 添加条件时 | 提示切换模式 | "条件过多，建议切换到 SQL 模式" |

### SQL 注入防护

**防护策略**：
- 所有标识符（表名、列名）用双引号包裹并转义
- 所有字符串值用单引号包裹并转义
- 数值类型严格校验，禁止非数字字符
- 禁止在值中使用 SQL 关键字（如 DROP, DELETE）

**转义函数**：
```typescript
function escapeSqlIdentifier(name: string): string {
  return `"${name.replace(/"/g, '""')}"`;
}

function escapeSqlString(value: string): string {
  return `'${value.replace(/'/g, "''")}'`;
}
```

---

## 🎨 UI/UX 规范

### 设计模式: Scatter & Gather

**Scatter (散点定义)**:
- 入口点分散在每个表的每个列上
- 用户在数据上下文中直接定义条件

**Gather (线性组装)**:
- 所有条件汇聚到统一的底部 FilterBar
- 支持拖拽排序、分组、删除

### 视觉规范

| 元素 | 样式 |
|------|------|
| 条件 Chip | Shadcn Badge (outline), 可点击编辑 |
| 逻辑连接符 | 小文本按钮 `AND` / `OR`，点击切换 |
| 分组 | 左边框高亮 + 缩进 |
| 模式切换 | Toggle Button Group |

### 交互规范

| 操作 | 行为 |
|------|------|
| 添加条件 | 列图标 → Popover → 填写 → 添加到 Bar |
| 编辑条件 | 点击 Chip → 弹出 Popover |
| 删除条件 | Chip 右侧 X 按钮 |
| 创建分组 | 拖拽 A 到 B 上方/下方 → 自动生成 (A op B) |
| 切换逻辑 | 点击连接符文本 AND ↔ OR |

---

## 📊 数据模型

```typescript
// 操作符类型
type FilterOperator =
  | '=' | '!=' | '>' | '>=' | '<' | '<='
  | 'LIKE' | 'NOT LIKE'
  | 'IN' | 'NOT IN'
  | 'IS NULL' | 'IS NOT NULL';

// 逻辑连接符
type LogicOperator = 'AND' | 'OR';

// 单个条件节点
interface FilterCondition {
  id: string;
  type: 'condition';
  table: string;
  column: string;
  operator: FilterOperator;
  value: any; // string | number | (string|number)[]
}

// 逻辑分组节点
interface FilterGroup {
  id: string;
  type: 'group';
  logic: LogicOperator;
  children: FilterNode[];
}

// 原始 SQL 节点（降级兜底）
interface FilterRaw {
  id: string;
  type: 'raw';
  sql: string;
}

// 联合类型
type FilterNode = FilterCondition | FilterGroup | FilterRaw;
```

---

## ✅ 验收检查清单

### 功能验收
- [ ] 可从 TableCard 列添加筛选条件
- [ ] 支持 `=`, `!=`, `>`, `<`, `>=`, `<=`, `LIKE`, `IN`, `IS NULL` 等操作符
- [ ] 支持 AND/OR 逻辑切换
- [ ] 支持拖拽创建分组
- [ ] 支持可视化 ↔ SQL 模式切换
- [ ] SQL 解析失败时降级为 Raw 块
- [ ] 特殊字符正确转义
- [ ] 数据类型校验生效

### 兼容性验收
- [ ] 与现有 JoinQueryPanel 状态兼容
- [ ] 生成的 WHERE 子句可直接拼接到最终 SQL
- [ ] 暗色模式适配
- [ ] 移动端基本可用

### 性能验收
- [ ] 50 个条件渲染无卡顿
- [ ] 模式切换响应 < 100ms
- [ ] SQL 解析超时正确降级

### 无障碍性验收
- [ ] 键盘可完整操作（Tab, Enter, Escape, Delete）
- [ ] 屏幕阅读器可正确朗读
- [ ] ARIA 属性完整
- [ ] 焦点管理正确

### 错误处理验收
- [ ] 所有错误场景有友好提示
- [ ] SQL 注入尝试被正确阻止
- [ ] 类型不匹配有明确提示
- [ ] 解析失败不影响现有条件

---

## 🌐 国际化 Key 列表

### 基础操作
- `filter.addCondition`: 添加条件 / Add Condition
- `filter.editCondition`: 编辑条件 / Edit Condition
- `filter.deleteCondition`: 删除条件 / Delete Condition
- `filter.createGroup`: 创建分组 / Create Group
- `filter.ungroupConditions`: 解散分组 / Ungroup Conditions

### 模式切换
- `filter.modes.visual`: 可视化 / Visual
- `filter.modes.sql`: SQL / SQL
- `filter.switchToVisual`: 切换到可视化 / Switch to Visual
- `filter.switchToSql`: 切换到 SQL / Switch to SQL

### 逻辑连接符
- `filter.logic.and`: 并且 / AND
- `filter.logic.or`: 或者 / OR
- `filter.logic.toggle`: 切换逻辑 / Toggle Logic

### 操作符
- `filter.operator.equals`: 等于 / Equals (=)
- `filter.operator.notEquals`: 不等于 / Not Equals (≠)
- `filter.operator.greaterThan`: 大于 / Greater Than (>)
- `filter.operator.greaterThanOrEqual`: 大于等于 / Greater Than or Equal (≥)
- `filter.operator.lessThan`: 小于 / Less Than (<)
- `filter.operator.lessThanOrEqual`: 小于等于 / Less Than or Equal (≤)
- `filter.operator.like`: 包含 / Contains (LIKE)
- `filter.operator.notLike`: 不包含 / Not Contains (NOT LIKE)
- `filter.operator.in`: 在列表中 / In List (IN)
- `filter.operator.notIn`: 不在列表中 / Not In List (NOT IN)
- `filter.operator.isNull`: 为空 / Is NULL
- `filter.operator.isNotNull`: 不为空 / Is Not NULL

### 表单字段
- `filter.form.table`: 表 / Table
- `filter.form.column`: 列 / Column
- `filter.form.operator`: 操作符 / Operator
- `filter.form.value`: 值 / Value
- `filter.form.values`: 值列表 / Values
- `filter.form.placeholder.selectTable`: 选择表 / Select Table
- `filter.form.placeholder.selectColumn`: 选择列 / Select Column
- `filter.form.placeholder.enterValue`: 输入值 / Enter Value
- `filter.form.placeholder.enterValues`: 输入值，回车添加 / Enter values, press Enter to add

### 错误提示
- `filter.error.parseError`: SQL 解析失败，已保留原始内容 / Failed to parse SQL, original content preserved
- `filter.error.required`: 请填写此字段 / This field is required
- `filter.error.invalidNumber`: 请输入有效数字 / Please enter a valid number
- `filter.error.invalidInteger`: 请输入有效整数 / Please enter a valid integer
- `filter.error.invalidDate`: 日期格式无效，请使用 YYYY-MM-DD / Invalid date format, use YYYY-MM-DD
- `filter.error.invalidTimestamp`: 时间格式无效 / Invalid timestamp format
- `filter.error.tooManyConditions`: 条件过多（{count}），建议切换到 SQL 模式 / Too many conditions ({count}), consider switching to SQL mode
- `filter.error.tooDeepNesting`: 分组层级不能超过 {max} 层 / Nesting level cannot exceed {max}
- `filter.error.valueTooLong`: 输入内容过长，已截断至 {max} 字符 / Input too long, truncated to {max} characters

### 占位提示
- `filter.emptyPlaceholder`: 暂无筛选条件，点击列名旁的图标添加 / No filters yet, click column icon to add
- `filter.rawSqlBlock`: 原始 SQL 块（无法解析） / Raw SQL Block (Cannot Parse)

### 操作按钮
- `filter.action.confirm`: 确定 / Confirm
- `filter.action.cancel`: 取消 / Cancel
- `filter.action.clear`: 清空 / Clear
- `filter.action.clearAll`: 清空所有条件 / Clear All Conditions
