# 新 UI 组件改造验证标准

## 📋 文档说明

**用途**: 用于验证新 UI 组件改造是否符合项目标准  
**适用范围**: 所有 `frontend/src/new/` 目录下的组件  
**版本**: v1.0.0  
**更新日期**: 2024-12-02

---

## 🎯 验证标准概览

### 验证层级
```
1. 代码层面验证（自动化）
   ├─ 文件结构检查
   ├─ 代码规范检查
   ├─ 构建验证
   └─ 类型检查

2. 功能层面验证（自动化 + 手动）
   ├─ Toast 通知系统
   ├─ 数据刷新机制
   ├─ 错误处理
   └─ 用户交互

3. 用户体验验证（手动）
   ├─ 视觉效果
   ├─ 交互流畅度
   ├─ 响应速度
   └─ 错误提示友好度
```

---

## 📝 验证清单

### 第一部分：代码层面验证（自动化）

#### 1.1 文件结构检查 ✅

**检查项**:
- [ ] 组件文件位于 `frontend/src/new/` 目录
- [ ] 使用 `.tsx` 扩展名（TypeScript + React）
- [ ] 文件命名使用 PascalCase（如 `DataPasteCard.tsx`）
- [ ] 相关组件放在同一目录下

**验证命令**:
```bash
# 检查文件是否存在
ls -la frontend/src/new/DataSource/ComponentName.tsx

# 检查文件扩展名
find frontend/src/new -name "*.tsx" | grep ComponentName
```

**通过标准**: 所有文件结构符合项目规范

---

#### 1.2 代码规范检查 ✅

**检查项**:
- [ ] 使用 shadcn/ui 组件（Card, Button, Input 等）
- [ ] 使用语义化 Tailwind 类名（`bg-surface`, `text-foreground`）
- [ ] 禁止硬编码颜色（`#fff`, `rgb()`, `bg-blue-500`）
- [ ] 禁止直接使用 CSS 变量（`var(--dq-*)`）
- [ ] 使用 i18n 翻译（`t("key")`）
- [ ] Props 有明确类型定义

**验证命令**:
```bash
# 检查硬编码颜色
grep -E "#[0-9a-fA-F]{3,6}|rgb\(|hsl\(|bg-blue-|bg-red-|bg-green-" frontend/src/new/DataSource/ComponentName.tsx

# 检查 CSS 变量直接使用
grep "var(--dq-" frontend/src/new/DataSource/ComponentName.tsx

# 检查 i18n 使用
grep "t(\"" frontend/src/new/DataSource/ComponentName.tsx
```

**通过标准**: 
- 无硬编码颜色
- 无直接使用 CSS 变量
- 所有用户可见文本使用 i18n

---

#### 1.3 构建验证 ✅

**检查项**:
- [ ] `npm run build` 成功
- [ ] 无 TypeScript 错误
- [ ] 无 ESLint 错误
- [ ] 打包体积合理

**验证命令**:
```bash
cd frontend
npm run build
```

**通过标准**: 
- 构建成功，退出码为 0
- 无错误输出
- 打包文件生成在 `dist/` 目录

---

#### 1.4 类型检查 ✅

**检查项**:
- [ ] Props 有 TypeScript 类型定义
- [ ] 函数参数有类型注解
- [ ] 状态变量有明确类型
- [ ] 无 `any` 类型滥用

**验证命令**:
```bash
# 检查 TypeScript 错误
npx tsc --noEmit

# 或使用 IDE 的诊断功能
```

**通过标准**: 无 TypeScript 类型错误

---

### 第二部分：功能层面验证

#### 2.1 Toast 通知系统 ✅

**检查项**:
- [ ] 组件接收 `showNotification` prop
- [ ] 定义 `notify` 函数
- [ ] 所有成功操作显示成功 Toast（绿色）
- [ ] 所有失败操作显示错误 Toast（红色）
- [ ] 输入验证失败显示警告 Toast（橙色）
- [ ] Toast 消息使用 i18n 翻译

**代码模式**:
```typescript
// ✅ 正确模式
const ComponentName = ({ onSuccess, showNotification }) => {
  const notify = (message, severity = "info") => {
    if (!message) return;
    showNotification?.(message, severity);
  };

  const handleSuccess = () => {
    const successMsg = t("success.message");
    notify(successMsg, "success");
  };

  const handleError = (err) => {
    const errorMsg = t("error.message", { error: err.message });
    notify(errorMsg, "error");
  };
};
```

**验证命令**:
```bash
# 检查 notify 函数定义
grep "const notify = " frontend/src/new/DataSource/ComponentName.tsx

# 检查 notify 调用次数
grep -c "notify(" frontend/src/new/DataSource/ComponentName.tsx

# 检查 showNotification prop
grep "showNotification" frontend/src/new/DataSource/ComponentName.tsx
```

**通过标准**: 
- notify 函数已定义
- 所有关键操作都调用 notify
- showNotification prop 正确接收

**手动验证**:
1. 执行成功操作 → 看到绿色 Toast
2. 执行失败操作 → 看到红色 Toast
3. 输入验证失败 → 看到橙色 Toast
4. Toast 在屏幕右上角显示
5. Toast 3-5秒后自动消失

---

#### 2.2 数据刷新机制 ✅

**检查项**:
- [ ] 组件接收 `onDataSourceSaved` 或类似回调
- [ ] 操作成功后调用回调触发刷新
- [ ] 父组件传递 `triggerRefresh` 函数
- [ ] 刷新后数据源列表自动更新

**代码模式**:
```typescript
// ✅ 正确模式
const ComponentName = ({ onDataSourceSaved }) => {
  const handleSave = async () => {
    // ... 保存逻辑
    if (result.success) {
      onDataSourceSaved?.({
        id: tableName,
        name: tableName,
        sourceType: "duckdb",
        type: "table"
      });
    }
  };
};

// 父组件调用
<ComponentName 
  onDataSourceSaved={triggerRefresh}
/>
```

**验证命令**:
```bash
# 检查回调 prop
grep "onDataSourceSaved" frontend/src/new/DataSource/ComponentName.tsx

# 检查回调调用
grep "onDataSourceSaved?\.(" frontend/src/new/DataSource/ComponentName.tsx
```

**通过标准**: 
- 回调 prop 已接收
- 成功操作后调用回调

**手动验证**:
1. 执行操作（上传、保存等）
2. 操作成功后数据源列表自动刷新
3. 新数据立即显示在列表中

---

#### 2.3 错误处理 ✅

**检查项**:
- [ ] 所有 API 调用有 try-catch
- [ ] 网络错误有友好提示
- [ ] 验证错误有清晰说明
- [ ] 错误消息使用 i18n
- [ ] 错误状态正确显示

**代码模式**:
```typescript
// ✅ 正确模式
const handleSubmit = async () => {
  // 输入验证
  if (!input.trim()) {
    const errorMsg = t("error.required");
    setError(errorMsg);
    notify(errorMsg, "warning");
    return;
  }

  try {
    const response = await apiCall();
    if (response.success) {
      // 成功处理
    } else {
      const errorMsg = response.error || t("error.unknown");
      setError(errorMsg);
      notify(errorMsg, "error");
    }
  } catch (err) {
    const errorMsg = t("error.network", { message: err.message });
    setError(errorMsg);
    notify(errorMsg, "error");
  }
};
```

**验证命令**:
```bash
# 检查 try-catch 使用
grep -A 5 "try {" frontend/src/new/DataSource/ComponentName.tsx | grep "catch"

# 检查错误处理
grep "setError\|notify.*error" frontend/src/new/DataSource/ComponentName.tsx
```

**通过标准**: 
- 所有异步操作有错误处理
- 错误消息清晰友好

**手动验证**:
1. 断网后执行操作 → 看到网络错误提示
2. 输入错误数据 → 看到验证错误提示
3. 错误消息清晰易懂

---

#### 2.4 用户交互 ✅

**检查项**:
- [ ] 按钮有 loading 状态
- [ ] 按钮有 disabled 状态
- [ ] 输入框有 focus 样式
- [ ] 表单有验证反馈
- [ ] 操作有即时反馈

**代码模式**:
```typescript
// ✅ 正确模式
const [loading, setLoading] = useState(false);

<Button 
  onClick={handleSubmit}
  disabled={loading || !isValid}
>
  {loading ? t("actions.loading") : t("actions.submit")}
</Button>

<Input
  value={value}
  onChange={handleChange}
  className="focus:ring-2 focus:ring-primary"
/>
```

**通过标准**: 
- 所有交互元素有正确状态
- 用户操作有即时反馈

**手动验证**:
1. 点击按钮 → 按钮显示 loading 状态
2. 输入框获得焦点 → 显示 focus 样式
3. 提交表单 → 立即显示反馈

---

### 第三部分：用户体验验证（手动）

#### 3.1 视觉效果 ✅

**检查项**:
- [ ] 组件样式符合设计系统
- [ ] 明暗模式都正常显示
- [ ] 颜色对比度达标
- [ ] 圆角、阴影、间距一致
- [ ] 响应式布局正常

**验证步骤**:
1. 切换明暗模式 → 样式正常
2. 调整窗口大小 → 布局自适应
3. 检查颜色对比度 → 文字清晰可读
4. 对比其他组件 → 样式一致

**通过标准**: 
- 视觉效果符合设计系统
- 明暗模式都正常
- 响应式布局正常

---

#### 3.2 交互流畅度 ✅

**检查项**:
- [ ] 动画流畅（无卡顿）
- [ ] 过渡自然（duration-fast/normal/slow）
- [ ] 反馈及时（< 100ms）
- [ ] 无闪烁或跳动

**验证步骤**:
1. 执行各种操作 → 动画流畅
2. 切换状态 → 过渡自然
3. 点击按钮 → 反馈及时

**通过标准**: 
- 所有交互流畅自然
- 无明显性能问题

---

#### 3.3 响应速度 ✅

**检查项**:
- [ ] 页面加载 < 2s
- [ ] 操作响应 < 500ms
- [ ] API 调用有 loading 提示
- [ ] 大数据量不卡顿

**验证步骤**:
1. 刷新页面 → 快速加载
2. 执行操作 → 快速响应
3. 上传大文件 → 有进度提示

**通过标准**: 
- 响应速度符合预期
- 无明显延迟

---

#### 3.4 错误提示友好度 ✅

**检查项**:
- [ ] 错误消息清晰易懂
- [ ] 提供解决建议
- [ ] 避免技术术语
- [ ] 使用用户语言

**验证步骤**:
1. 触发各种错误 → 消息清晰
2. 检查错误提示 → 有解决建议
3. 检查用户理解度 → 易于理解

**通过标准**: 
- 错误消息用户友好
- 提供有用的帮助信息

---

## 🚀 快速验证流程

### 自动化验证（5分钟）

```bash
# 1. 运行验证脚本
.kiro/specs/VERIFICATION_STANDARD_CHECK.sh ComponentName

# 2. 检查输出
# ✅ 所有检查通过 → 继续手动验证
# ❌ 有检查失败 → 修复问题后重新验证
```

### 手动验证（10分钟）

```
1. Toast 通知（3分钟）
   - 成功操作 → 绿色 Toast ✅
   - 失败操作 → 红色 Toast ✅
   - 验证失败 → 橙色 Toast ✅

2. 数据刷新（2分钟）
   - 操作成功 → 列表刷新 ✅
   - 新数据显示 → 立即可见 ✅

3. 错误处理（2分钟）
   - 网络错误 → 友好提示 ✅
   - 验证错误 → 清晰说明 ✅

4. 用户体验（3分钟）
   - 明暗模式 → 都正常 ✅
   - 交互流畅 → 无卡顿 ✅
   - 响应及时 → 无延迟 ✅
```

---

## 📊 验证报告模板

### 组件验证报告

**组件名称**: ___________  
**验证日期**: ___________  
**验证人**: ___________

#### 自动化验证结果

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 文件结构 | ☐ 通过 ☐ 失败 | |
| 代码规范 | ☐ 通过 ☐ 失败 | |
| 构建验证 | ☐ 通过 ☐ 失败 | |
| 类型检查 | ☐ 通过 ☐ 失败 | |

#### 功能验证结果

| 检查项 | 状态 | 备注 |
|--------|------|------|
| Toast 通知 | ☐ 通过 ☐ 失败 | |
| 数据刷新 | ☐ 通过 ☐ 失败 | |
| 错误处理 | ☐ 通过 ☐ 失败 | |
| 用户交互 | ☐ 通过 ☐ 失败 | |

#### 用户体验验证结果

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 视觉效果 | ☐ 通过 ☐ 失败 | |
| 交互流畅度 | ☐ 通过 ☐ 失败 | |
| 响应速度 | ☐ 通过 ☐ 失败 | |
| 错误提示 | ☐ 通过 ☐ 失败 | |

#### 总体评价

- **通过项**: _____ / 12
- **失败项**: _____ / 12
- **通过率**: _____%

**结论**: ☐ 通过验证 ☐ 需要修复

**备注**:
___________________________________________
___________________________________________

---

## 🔧 常见问题修复指南

### 问题 1: Toast 不显示

**原因**:
- showNotification prop 未传递
- notify 函数未调用
- Toast 组件未正确配置

**修复**:
1. 检查 prop 传递
2. 检查 notify 调用
3. 检查 ToastProvider

### 问题 2: 数据不刷新

**原因**:
- 回调未调用
- triggerRefresh 未传递
- 缓存未清理

**修复**:
1. 检查回调调用
2. 检查 prop 传递
3. 调用 clearCache

### 问题 3: 样式不正确

**原因**:
- 使用了硬编码颜色
- 使用了 CSS 变量
- 未使用语义化类名

**修复**:
1. 替换为语义化类名
2. 移除硬编码颜色
3. 移除 CSS 变量

### 问题 4: TypeScript 错误

**原因**:
- Props 类型未定义
- 参数类型缺失
- any 类型滥用

**修复**:
1. 添加 Props 接口
2. 添加参数类型
3. 替换 any 为具体类型

---

## 📚 参考文档

### 设计系统
- [AGENTS.md](file:.kiro/steering/AGENTS.md) - UI/CSS/主题规范
- [tailwind.css](file:frontend/src/styles/tailwind.css) - CSS 变量定义
- [tailwind.config.js](file:frontend/tailwind.config.js) - Tailwind 配置

### 组件示例
- [DataPasteCard.tsx](file:frontend/src/new/DataSource/DataPasteCard.tsx) - 完整示例
- [UploadPanel.tsx](file:frontend/src/new/DataSource/UploadPanel.tsx) - Toast 示例
- [DatabaseForm.tsx](file:frontend/src/new/DataSource/DatabaseForm.tsx) - 表单示例

### 测试文档
- [QUICK_TEST_CHECKLIST.md](file:.kiro/specs/excel-sheet-selector-fix/QUICK_TEST_CHECKLIST.md) - 快速测试
- [AUTOMATED_VERIFICATION.md](file:.kiro/specs/excel-sheet-selector-fix/AUTOMATED_VERIFICATION.md) - 自动化验证

---

## 🎯 验证标准版本历史

### v1.0.0 (2024-12-02)
- 初始版本
- 基于 DataPasteCard 改造经验提取
- 包含自动化和手动验证标准

---

**使用建议**: 
1. 每次改造新组件前，先阅读本标准
2. 改造完成后，按照本标准逐项验证
3. 填写验证报告，记录验证结果
4. 发现问题及时修复，重新验证

**持续改进**: 
- 根据实际使用情况更新标准
- 添加新的验证项和最佳实践
- 优化验证流程，提高效率
